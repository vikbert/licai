

```bash
# then access http://localhost, the default symfony application should be host on port 80
docker-compose up -d

# create a default symfony application: console and api app
# use --full, to create a fullstack classical symfony web app
symfony new app

# config php, nginx, node, redis, db in docker-compose.yml
touch docker-compose.yml

# push repo to remote
git push --set-upstream origin master
hub create licai
gam 'init commit'
git add .
touch README.md
git init
cd licai/
mkdir licai 
```
